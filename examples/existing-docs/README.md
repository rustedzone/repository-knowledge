# Existing documentation example

This example represents a legacy or mixed repository with useful pre-existing documentation. Installation preserves `docs/index.md` and the system overview; the team adds repository metadata that routes agents to them instead of moving content into a prescribed taxonomy.
