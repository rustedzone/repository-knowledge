Improve the documentation under `docs/` so that a new engineer can safely add a protected, form-backed user-management page without rediscovering the repository architecture from source.

Establish the current framework version, explain how protected navigation and permissions work, trace the user form through validation and server interaction, explain the API proxy boundary, and distinguish static data from live integrations. Include short, source-attributed examples for recurring implementation patterns and a clear reading path for onboarding.

Verify every material claim against current repository evidence. Keep all changes under `docs/`, preserve application files, and complete the documentation improvement in this task.
