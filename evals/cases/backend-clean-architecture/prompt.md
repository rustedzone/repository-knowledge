Use repository-knowledge to rebuild the repository documentation now.

This is a generation request, not an assessment request. Inspect the complete repository, classify every applicable repository profile, build the required coverage ledger and trace dossiers, and revise the existing shallow documents in this task.

Trace each material request or lifecycle through concrete symbols, including identity and trust boundaries, middleware, controllers, use cases, repositories, stores, integrations, responses, state changes, failures, and tests. Inspect all approval-state writes and group-deletion behavior across migrations, application cleanup, and external authorization synchronization. Include faithful source-derived examples for recurring extension seams.

Apply the anti-speculation, implementation-readiness, routing, and remediation gates. Do not stop after scan, CLI rebuild, critique, or a package/controller catalog. Keep semantic documentation under `docs/`, update verified capability routes, and do not modify application source.
