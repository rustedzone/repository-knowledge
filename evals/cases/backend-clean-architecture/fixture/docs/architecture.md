# Architecture

The service follows Clean Architecture. Controllers are in `internal/adapter/http/controller`, use cases are in `internal/usecase`, and repositories are in `internal/adapter/postgres`.
