# Access Dashboard fixture

This small Next.js fixture exists to evaluate repository documentation generation. Run `npm run dev` for local development and `npm test` for tests.

The authoritative implementation is under `src/`. Some agent notes are intentionally stale.
